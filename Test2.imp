procedure Bar() returns (int x, int y)
    ensures y == 10
{
    if (foo(x, y)) {
        int x;
        int b;
        int z = 10 + 20;
        if (z > 10) {
            b = x > 10;
        }
        while (x == z)
            invariant y == z
            invariant forall x :: x > 10
        {
        }
    }
}

