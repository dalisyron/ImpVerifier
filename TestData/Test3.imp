method Bar()
    ensures y == 10
    requires x == (z + 10) * 20
{
    if (foo(x, y)) {
        int x;
        int b;
        int z = 10 + 20;
        if (z > 10) {
            b = x > 10;
        }
        while (x == z)
            invariant y == z
            invariant forall (bool x) :: x > 10
        {
        
        }
    }
    
    bool x = a;
    bool x = foo(a);
    bool z = foo(z || (z + 1));
}

