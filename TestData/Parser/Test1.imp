method Foo() returns (int x)
    ensures y == 10
{
    if (true) {
    }
    int x;

    x = 10;

    int[] a = new int[3];

    a[3] = 10;

    int y = x;
    if (x > 10) {
        y = y + 1;
    }
    
    int z;
    bool zzz;
    while (z > x) 
    {
        z = x - y;
    }
}

method Bar() returns (int x)
    ensures y == 10
{
    t1 = Foo(2);
    // Hello world
    int x = 10;
    
    while (x > 10) {
        x = x + 10;
        
        if (x > (y + z)) {
        }    
    }
}
